#include<stdio.h>

void roundoff(float nm)
{
	int digits;
	float limit=0.5;
	printf("Round upto: ");
	scanf("%d",&digits);
	printf("%.*f",digits,nm);
}

int main()
{
	float nm;
	printf("Enter the Number :");
	scanf("%f",&nm);
	roundoff(nm);
}
